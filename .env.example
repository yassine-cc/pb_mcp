# PocketBase MCP Server Configuration
# Copy this file to .env and update the values

# ===========================================
# PocketBase Connection Settings
# ===========================================

# PocketBase server URL
# Default: http://127.0.0.1:8090
POCKETBASE_URL=http://127.0.0.1:8090

# Admin authentication token for privileged operations
# Required for: collection management, user management, and admin-only record operations
# Get this token by authenticating as admin via the PocketBase Admin UI or API
# Note: If you provide POCKETBASE_ADMIN_EMAIL and POCKETBASE_ADMIN_PASSWORD below,
# the server will auto-authenticate and populate this token at startup
POCKETBASE_ADMIN_TOKEN=your_admin_token_here

# ===========================================
# Admin Auto-Authentication (Optional)
# ===========================================
# If both email and password are provided, the server will automatically
# authenticate as admin at startup and obtain a token.
# This is useful when you don't have a pre-existing token.

# Admin email address
# POCKETBASE_ADMIN_EMAIL=admin@example.com

# Admin password
# POCKETBASE_ADMIN_PASSWORD=your_admin_password

# ===========================================
# Output Format Settings
# ===========================================

# Output format for tool responses
# Options: json (default), toon
# TOON format reduces token usage by 30-60% when communicating with LLMs
MCP_OUTPUT_FORMAT=json

# ===========================================
# Development Settings
# ===========================================

# Node environment
# Options: development, production, test
NODE_ENV=development
